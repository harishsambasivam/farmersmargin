import React from 'react';
import PropTypes from 'prop-types';
import './Home.scss';

const Home = () => (
  <div className="Home" data-testid="Home">
    Home Component
  </div>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
